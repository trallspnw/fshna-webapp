{
  "name": "site",
  "version": "1.0.0",
  "description": "Static frontend for the FSHNA website.",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "dev": "APP_ENV=site pnpm build && pnpm downloadMedia && serve out -l 3001",
    "downloadMedia": "tsx src/scripts/downloadMedia.ts",
    "build": "next build"
  }
}
