{
  "name": "site",
  "version": "1.0.0",
  "description": "Static frontend for the FSHNA website.",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "dev": "pnpm build && serve out -l 3001",
    "downloadMedia": "tsx src/scripts/downloadMedia.ts",
    "build": "APP_ENV=site next build && pnpm downloadMedia"
  }
}
